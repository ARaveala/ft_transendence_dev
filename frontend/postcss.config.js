module.exports = {
  plugins: {
    tailwindcss: {},          // TailwindCSS plugin: transforms Tailwind classes into actual CSS
    autoprefixer: {},         // Autoprefixer plugin: automatically adds vendor prefixes
  },                          // (e.g., -webkit-, -moz-) to ensure CSS works across different browsers
}
